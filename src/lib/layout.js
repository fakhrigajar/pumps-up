export const SALE_PANEL_HEIGHT = 122

/**
 * How tall the sale panel gets when it is pulled open. A viewport share so it
 * fits a laptop and a shop screen alike, capped so it never becomes a full
 * screen the product list has vanished behind — the drawer is for reviewing
 * the basket, and the till underneath should stay recognisable.
 */
export const SALE_PANEL_EXPANDED = 'min(64vh, 480px)'
